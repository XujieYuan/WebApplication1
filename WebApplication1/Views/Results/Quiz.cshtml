@model IEnumerable<WebApplication1.Models.Result>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- bradcam_area_start -->
<div class="bradcam_area breadcam_bg_a overlay">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h3>Results</h3>
            </div>
        </div>
    </div>
</div>
<!-- bradcam_area_end -->

@*<h2>Index</h2>*@

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Answer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Count)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Answer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Count)
            </td>
            @*<td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>*@
        </tr>
    }

</table>
<a href="~/Results/Intro" class="btn btn-success" style="margin-left:10px; background-color:#12b322; border-color:#12b322;">Return</a>
<a href="#" class="btn btn-success" style="margin-left:10px; background-color:#12b322; border-color:#12b322;">Continue</a>
    